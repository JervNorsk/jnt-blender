[project]
name = "jnt"
version = "2025.2.1"
requires-python = ">=3.11"
dependencies = [ "jnt_sandbox",]

[tool.uv]
dev-dependencies = [ "toml",]

[tool.uv.workspace]
members = [ "libs/*", "packages/*",]

[tool.uv.sources.jnt_sandbox]
workspace = true
